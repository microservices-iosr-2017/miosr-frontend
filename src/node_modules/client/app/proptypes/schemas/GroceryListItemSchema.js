import { PropTypes } from 'react'
import schema from './Schema'

const { string, number, bool, oneOfType } = PropTypes;

const ItemSchema = {
  _id:       oneOfType([string, number]).isRequired,
  name:      string.isRequired,
  completed: bool
};

export default schema(ItemSchema);
