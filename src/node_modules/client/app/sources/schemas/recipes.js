import {Schema, arrayOf} from 'normalizr';

const recipe = new Schema('recipes', {idAttribute: '_id'});
const ingredient = new Schema('ingredients', {idAttribute: '_id'});

export const recipeWithIngredients = new Schema('recipes', {idAttribute: '_id'});
recipeWithIngredients.define({
  ingredients: arrayOf(ingredient)
});

export default recipe;
