import React, {PropTypes} from 'react';
import {Grid, Row} from 'react-bootstrap';
import {Link} from 'react-router';
import RecipeSchema from 'schemas/RecipeSchema';

import Cards from 'components/shared/card/Cards';
import ActionButton from 'components/shared/ActionButton';
import RecipeCard from './RecipeCard';

const RecipesView = ({recipes, onRecipeClick}) => (
  <Grid className="c__recipes">
    <Link to="/recipes/new">
      <ActionButton icon="plus" />
    </Link>
    <Row>
      <Cards>
        {recipes.map(recipe => (
          <RecipeCard recipe={recipe}
                      onClick={() => onRecipeClick(recipe._id)}
                      key={recipe._id} />))}
      </Cards>
    </Row>
  </Grid>
);

RecipesView.propTypes = {
  recipes: PropTypes.arrayOf(RecipeSchema.only('_id')).isRequired,
  onRecipeClick: PropTypes.func,
};

RecipesView.defaultProps = {
  onRecipeClick: () => {},
};

export default RecipesView;
