import alt from 'alt-instance';

class RecipeActions {
  searchRecipes = (query) => query;
}

export default {
  ...alt.generateActions('fetchRecipes', 'fetchSuccess', 'fetchFailure', 'addRecipe'),
  ...alt.createActions(RecipeActions),
};
