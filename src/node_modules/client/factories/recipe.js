import {Factory} from 'rosie';

export default new Factory()
  .sequence('_id')
  .option('withImage', false)
  .attr('image', ['withImage'], (withImage) => withImage ? 'abcdef.png' : undefined)
  .attrs({
    title: 'Recipe',
    subtitle: 'Subtitle',
    description: 'A recipe',
  });
