
class AppActionCreators {
  login(username, password) {return {username, password};}
  loginSuccess(token) {return {token};}
  loginFailure(cause) {return {cause};}
  // loggedOut
}

export default function build({altInstance}) {
  return altInstance.createActions(AppActionCreators);
}
