
class NotesActionCreators {
  fetch() {return {};}
  fetchSuccess(payload) {return payload;}
  fetchFailure(error) {return error;}

  select(id) {return id;}
  deselect() {return {};}

  add() {return {};}
}

export default function build({altInstance}) {
  return altInstance.createActions(NotesActionCreators);
}
