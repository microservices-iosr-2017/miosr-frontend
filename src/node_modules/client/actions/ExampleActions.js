import alt from 'client/alt';

class ExampleActionCreators {
  fetchExample() {return {};}
  fetchExampleSuccess(payload) {return payload;}
  fetchExampleSuccess(error) {return error;}
}

export default alt.createActions(ExampleActionCreators);
