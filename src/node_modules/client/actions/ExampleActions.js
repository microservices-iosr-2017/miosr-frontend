import alt from 'client/app/alt';

class ExamplesActionCreators {
  fetchExamples() {return {};}
  fetchExamplesSuccess(payload) {return payload;}
  fetchExamplesSuccess(error) {return error;}
}

export default alt.createActions(MealsActionCreators);
