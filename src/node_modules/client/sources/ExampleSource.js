
const MEALS_PATH = '/api/example';

export function createExampleSource(axios, actions) {
  return {
    fetch: () => {
      return axios.get(MEALS_PATH)
        .then(response => {
          const exampleReponse = response.data;
          actions.fetchExampleSuccess(exampleReponse);
        })
        .catch(error => {
          // access response via error.response
          actions.fetchExampleFailure(error);
        });
    }
  };
}
