'use strict';

var express = require('express');
var controller = require("./food_search.controller.js");

const router = express.Router();

router.get('/:name', controller.index);

export default router;
