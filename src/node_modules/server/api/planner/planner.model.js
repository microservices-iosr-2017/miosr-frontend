import mongoose from 'mongoose';
import {PlannerEntrySchema} from './entry/entry.model';

const Schema = mongoose.Schema;

const Planner = new Schema({
  date: {
    type: Date,
    required: true
  },
  entries: [PlannerEntrySchema],
});

export default mongoose.model('Planner', Planner);
